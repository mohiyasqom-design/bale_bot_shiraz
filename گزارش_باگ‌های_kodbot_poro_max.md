# گزارش کامل بررسی خط‌به‌خط `kodbot_poro_max.py`

**حجم فایل بررسی‌شده:** ۹٬۳۹۴ خط، ۲۷۷ تابع/کلاس
**روش بررسی:** خط‌به‌خط، از ابتدا تا انتهای فایل، شامل ردیابی جریان داده بین توابع و شماتای دیتابیس
**یادداشت مهم:** طبق درخواست، بخش «سیستم API Keys کدبات» (توابع `_generate_api_key`, `_validate_api_key`, `handle_api_keys_menu`, `handle_api_key_create`, `handle_api_start`, `_handle_api_bot_action` و مسیر `payload.startswith("api_")`) به‌طور کامل از این گزارش حذف شده چون قراره بازنویسی بشه.

---

## فهرست بر اساس شدت

- 🔴 **حیاتی** — باعث از دست رفتن داده، کرش کامل یک قابلیت، یا خسارت مالی می‌شه
- 🟠 **مهم** — یک قابلیت رو کاملاً یا تا حد زیادی از کار می‌ندازه
- 🟡 **متوسط** — باگ منطقی واقعی با تأثیر محدودتر یا شرطی
- ⚪ **جزئی / کد بو** — ریسک بالقوه، ناهماهنگی، یا کد زائد

---

## 🔴 باگ‌های حیاتی

### ۱. عدم sandbox برای اجرای کد آپلودی کاربر

**محل:** `start_bot_process` (نزدیک خط ۱۰۹۰–۱۱۵۰، دقیقاً جایی که `subprocess.Popen([sys.executable, file_path], ...)` صدا زده می‌شه)

کد پایتونی که کاربر آپلود می‌کنه مستقیماً با `subprocess.Popen([sys.executable, file_path])` اجرا می‌شه — یعنی با **همون دسترسی‌های پروسهٔ اصلی ربات**: توکن ربات، فایل دیتابیس SQLite، کل فایل‌سیستم قابل‌دسترسی، و شبکه. تنها لایهٔ محافظتی، یک چک متنی سطحی روی وجود رشته‌های `/app/data` یا `/mnt/data` در کد آپلودی هست (در `handle_deploy_file`، حوالی خط ۱۶۷۶) که به‌سادگی با ساختن رشته به‌صورت پویا (مثلاً `"/app" + "/data"`) یا از طریق `os.environ` قابل دور زدنه.

**تأثیر:** هر کاربری که بتونه یک فایل `.py` آپلود کنه (که این قابلیت اصلی و تبلیغ‌شدهٔ رباته) می‌تونه کد دلخواه روی سرور اجرا کنه — خوندن توکن ربات، دستکاری مستقیم دیتابیس همهٔ کاربران، یا استفاده از سرور برای هر هدف دیگه‌ای.

---

### ۲. تغییر قیمت پلن توسط ادمین بعد از ری‌استارت سرور بی‌اثر می‌شه (اما پنل ادمین همچنان قیمت غلط رو نشون می‌ده)

**محل:**
- تعریف اولیهٔ قیمت‌ها: دیکشنری `PLANS` (نزدیک خط ۲۴۸–۳۶۳، مقادیر hardcoded)
- هندلر تغییر قیمت: `st == "admin_edit_price"` در `handle_message`، خط ۸۶۴۸–۸۶۶۳
- نمایش قیمت به ادمین: `handle_change_plan_price` (خط ۳۷۳۹) و `handle_edit_price` (خط ۳۷۶۲)

وقتی ادمین قیمت یک پلن رو تغییر می‌ده، خط ۸۶۵۳–۸۶۵۴ **هم** `set_setting(f"price_{plan_key}_{duration}", ...)` (ذخیرهٔ دائمی در دیتابیس) **و هم** `PLANS[plan_key]["subscriptions"][duration]["price"] = new_price` (تغییر در حافظه) رو انجام می‌ده. مشکل این‌جاست: **هیچ‌جای دیگه‌ای از کد — نه در `init_db()`، نه در `main()`، نه هیچ نقطهٔ startup دیگه‌ای — این مقدار ذخیره‌شده در دیتابیس دوباره داخل `PLANS` بارگذاری نمی‌شه.**

نتیجه: 
1. بلافاصله بعد از تغییر قیمت توسط ادمین، همه‌چیز به نظر درست کار می‌کنه (چون `PLANS` in-memory هم آپدیت شده).
2. اما به‌محض هر ری‌استارت سرور (deploy جدید، کرش، ری‌استارت دستی — که کاملاً محتمله)، `PLANS` به مقادیر hardcoded اولیه برمی‌گرده، **در حالی که** `get_setting(...)` (که فقط در دو تابع نمایشی `handle_change_plan_price` و `handle_edit_price` استفاده می‌شه) هنوز قیمت «جدید» قدیمی رو برمی‌گردونه.
3. نتیجه: **پنل ادمین به ادمین می‌گه قیمت هنوز روی مقدار جدید تنظیم شده، در حالی که مبلغ واقعی که کاربر در چک‌اوت پرداخت می‌کنه (`handle_sub_menu`, `handle_view_plan`, `handle_checkout`, `handle_pay_now` — که همه مستقیماً از `PLANS[...]` می‌خونن) به مقدار پیش‌فرض کد برگشته.**

این یک مغایرت قیمتی خاموش با اثر مالی مستقیمه که هیچ خطا یا هشداری تولید نمی‌کنه.

---

### ۳. جریان پرداخت پنل «سازمانی» (Enterprise) کاملاً شکسته است

**محل:** `handle_enterprise_payment` (خط ۲۷۰۵–۲۷۲۹)

این تابع یک دیکشنری به نام `payment_payload` می‌سازه (شامل `chat_id`, `amount`, `description`, `payload`) اما **این متغیر هرگز به هیچ تابعی مثل `api(...)` یا هر تابع ارسال فاکتوری پاس داده نمی‌شه.** فقط ساخته می‌شه و رها می‌شه.

مشکل دوم و جدی‌تر: مبلغی که این تابع در `INSERT INTO enterprise_requests` (خط ۲۷۲۴) ثبت می‌کنه، یک عدد **هاردکد و ثابت (۵٬۰۰۰٬۰۰۰ تومان)** هست، در حالی که مبلغ واقعی enterprise باید بر اساس تعداد کاربر محاسبه بشه — چیزی که در `_handle_enterprise_user_count` (خط ۹۳۴۷–۹۳۸۱، با فرمول `total_price = base_price + (user_count * price_per_user)`) قبلاً محاسبه و به کاربر نشون داده شده.

**تأثیر:** کاربری که مراحل enterprise رو کامل می‌کنه، دکمهٔ «پرداخت» رو می‌زنه، ولی **هیچ فاکتوری دریافت نمی‌کنه** — هیچ راهی برای واقعاً پرداخت کردن وجود نداره. حتی اگه این باگ رفع بشه، مبلغ نمایش داده‌شده (۵ میلیون ثابت) با مبلغ محاسبه‌شدهٔ واقعی (که بر اساس تعداد کاربر متغیره) یکی نیست.

---

### ۴. تخفیف ۱۰۰٪ باعث ارسال فاکتور با مبلغ صفر می‌شه

**محل:** `handle_checkout` (نزدیک خط ۲۵۵۰، جایی که `"amount": final_price * 10` محاسبه می‌شه) و مسیر پرداخت `handle_pay_now`

با یک کد تخفیف ۱۰۰٪، `discount_val=100`، `amount_off = sub["price"]`، و در نتیجه `final_price = max(0, sub["price"] - amount_off) = 0`. کد **هیچ مسیر میان‌بری برای حالت `final_price == 0` نداره** که مستقیماً پلن رو فعال کنه بدون رفتن به مرحلهٔ ارسال فاکتور. در عوض، `sendInvoice` با `amount: 0` صدا زده می‌شه که رفتار نامشخصی داره (احتمالاً رد می‌شه توسط API بله) و کاربر در فرآیند پرداخت گیر می‌کنه.

---

### ۵. حذف پروژهٔ وب می‌تونه پروژهٔ اشتباه رو حذف کنه (استفاده از state قدیمی به‌جای callback data)

**محل:**
- Dispatcher: خط ۸۲۲۲–۸۲۲۴ (`elif data.startswith("web_delete:")`)
- تابع: `handle_web_delete_project` (خط ۵۷۳۰–۵۷۴۴)

خط ۸۲۲۳، `project_id` رو از خود callback data استخراج می‌کنه (`int(data.split(":")[1])`) اما این مقدار **هرگز به `handle_web_delete_project(query)` پاس داده نمی‌شه** — تابع فقط با یک آرگومان صدا زده می‌شه! خود تابع (خط ۵۷۳۴) به‌جاش `project_id` رو از `get_state(user_id)["data"]["project_id"]` می‌گیره.

**تأثیر:** اگه کاربر با بیش از یک پروژه تعامل داشته باشه (مثلاً پروژهٔ A رو باز کنه، بعد بره پروژهٔ B رو باز کنه، و بعد به یک پیام قدیمی که هنوز دکمهٔ حذف پروژهٔ A رو نشون می‌ده برگرده و روش کلیک کنه)، در واقع پروژه‌ای که در **state فعلی** کاربر (که ممکنه B باشه) قرار داره حذف می‌شه، نه پروژه‌ای که واقعاً در دکمه بود (A). چون این عملیات `DELETE FROM web_projects` هست و هیچ soft-delete یا امکان بازیابی وجود نداره، این یک ریسک واقعی از دست رفتن داده‌ست.

---

## 🟠 باگ‌های مهم

### ۶. قابلیت «کتابخانه اختصاصی» کاملاً غیرفعال است (نگاشت‌ها هرگز روی کد اعمال نمی‌شن)

**محل:** `process_custom_lib_code` (خط ۵۰۶۷–۵۰۷۲) و کل جریان `custom_lib_name`/`custom_lib_mappings` (خط ۸۷۱۹–۸۷۶۰)

منوی کامل «کتابخانه اختصاصی» وجود داره: کاربر می‌تونه یک نام دلخواه بسازه، نگاشت‌هایی مثل `درخواست=requests` وارد کنه، و ذخیره کنه. کد این نگاشت‌ها رو با موفقیت در جدول `custom_libraries` ذخیره می‌کنه (خط ۸۷۳۷–۸۷۴۲) و پیام موفقیت‌آمیز نشون می‌ده.

اما تابعی که واقعاً باید این نگاشت‌ها رو روی کد deploy‌شدهٔ ربات کاربر اعمال کنه (`process_custom_lib_code`) **فقط یک‌بار تعریف می‌شه و هیچ‌وقت در جای دیگری صدا زده نمی‌شه.** کل قابلیت از نظر UI کامل به نظر می‌رسه ولی از نظر عملکردی صرفاً تزئینیه — کاربر داده وارد می‌کنه که هیچ اثری روی هیچ‌چیز نداره.

---

### ۷. دکمه‌های «ساده / با سبک / تعاملی» در ساخت پروژهٔ وب کار نمی‌کنند

**محل:**
- دکمه‌ها: `handle_web_new_project` (خط ۵۴۳۲–۵۴۳۳)، با callback data `web_template:simple`, `web_template:styled`, `web_template:interactive`
- منطق ساخت: `elif st == "web_project_name"` در `handle_message` (خط ۹۰۳۷–۹۰۶۸)
- کلیدهای واقعی قالب: `WEB_TEMPLATES_DATA` (خط ۹۱۷۹–۹۱۹۸) که فقط `blog`, `portfolio`, `landing` داره

کلیدهایی که دکمه‌ها می‌فرستن (`simple`, `styled`, `interactive`) با کلیدهای واقعی موجود در `WEB_TEMPLATES_DATA` (`blog`, `portfolio`, `landing`) **هیچ تطابقی ندارن**. در نتیجه، در خط ۹۰۴۲–۹۰۴۳:
```python
template_type = state["data"].get("template", "blank")
tpl = WEB_TEMPLATES_DATA.get(template_type, {})
```
`tpl` همیشه یک دیکشنری خالی می‌شه، و `html_val`, `css_val`, `js_val` همه خالی می‌مونن. کاربر نام پروژه رو وارد می‌کنه، پروژه با محتوای کاملاً خالی ساخته می‌شه (نه با محتوای قالب انتخابی‌ش)، بدون هیچ پیام خطایی که بگه انتخابش اعمال نشد.

---

### ۸. توقف/راه‌اندازی مجدد ربات بعد از ری‌استارت سرور اصلی کار نمی‌کند (چه برای کاربر، چه برای ادمین)

**محل (همه از یک الگو پیروی می‌کنن):**
- کاربر عادی — دکمهٔ توقف: خط ۸۰۶۳–۸۰۷۱ (`elif data.startswith("stop_bot:")`)
- کاربر عادی — دکمهٔ ری‌استارت: خط ۸۰۷۲–۸۰۸۹ (`elif data.startswith("restart_bot:")`)
- ادمین: `handle_admin_stop_bot` (خط ۳۳۲۸–۳۳۴۳)
- ادمین: `handle_admin_restart_bot` (خط ۳۳۴۶–۳۳۶۸)
- ادمین: `handle_admin_delete_bot` (خط ۳۳۰۵ به بعد)

همهٔ این توابع فقط از دیکشنری در-حافظهٔ `bot_processes` (تعریف‌شده در خط ۲۰۵۳: `bot_processes: Dict[int, subprocess.Popen] = {}`) استفاده می‌کنن تا پروسهٔ در حال اجرای یک ربات رو پیدا کنن:
```python
proc = bot_processes.get(bot_id)
if proc:
    proc.terminate()
```
هیچ‌کدوم به `bot["pid"]` (که در ستون `pid` جدول `deployed_bots` به‌طور دائمی ذخیره شده — تعریف جدول در خط ۷۳۴۹) به‌عنوان fallback مراجعه نمی‌کنن.

**تأثیر:** `bot_processes` یک دیکشنری در حافظهٔ پایتونه که با هر ری‌استارت سرور اصلی (deploy جدید، کرش، ری‌استارت دستی) **کاملاً خالی می‌شه**، در حالی که پروسه‌های واقعی ربات‌های کاربران (که با `subprocess.Popen` جدا از پروسهٔ اصلی اجرا شدن) ممکنه همچنان روی سیستم‌عامل در حال اجرا باشن. بعد از چنین ری‌استارتی، **هیچ‌کس — نه کاربر، نه ادمین — نمی‌تونه از طریق UI ربات‌های در حال اجرا رو واقعاً متوقف کنه.** کلیک روی «توقف» فقط رکورد دیتابیس رو به `status='stopped'` تغییر می‌ده و به کاربر پیام موفقیت نشون می‌ده، اما پروسهٔ واقعی سیستم‌عامل بدون هیچ ردی در جایی همچنان زنده می‌مونه (orphan process).

---

### ۹. تناقض سیستماتیک نام ستون در جدول `web_projects` — از دست رفتن خاموش محتوای پروژه

**محل جدول:** `init_db()`، تعریف `CREATE TABLE web_projects` در خط ۷۳۷۷–۷۳۸۸ (ستون‌های واقعی: `project_name`, `html_code`, `css_code`, `js_code`) + یک migration در خط ۷۷۱۸–۷۷۳۲ که ستون‌های `html`, `css`, `js`, `name` رو هم با `ALTER TABLE ADD COLUMN ... DEFAULT ''` اضافه می‌کنه.

**توابعی که از ستون‌های "درست" (`html_code`/`css_code`/`js_code`/`project_name`) استفاده می‌کنن:**
- `handle_web_preview` (خط ۵۶۰۰–۵۶۰۶ تقریباً)
- بخش اصلی ساخت پروژه در `elif st == "web_project_name"` (خط ۹۰۳۷–۹۰۶۸ — این تابع هوشمندانه **هر دو مجموعه ستون رو با هم پر می‌کنه**، طبق کامنت صریح خودش در خط ۹۰۴۵–۹۰۴۶)

**توابعی که از ستون‌های «باقیمانده/legacy» (`html`/`css`/`js`/`name`) استفاده می‌کنن و هرگز ستون‌های اصلی رو نمی‌خونن یا آپدیت نمی‌کنن:**
- `handle_web_download_zip` (خط ۵۶۵۴–۵۶۷۸): `proj["html"]`, `proj["css"]`, `proj["js"]`, `proj['name']` — خطوط ۵۶۶۷، ۵۶۶۸، ۵۶۶۹، ۵۶۷۵
- `handle_web_download_html` (خط ۵۶۸۱–۵۷۰۴): `proj['name']`, `proj['css']`, `proj['html']`, `proj['js']` — خطوط ۵۶۹۲، ۵۶۹۳، ۵۶۹۴، ۵۶۹۵، ۵۷۰۱
- `handle_web_download_preview` (خط ۵۷۰۷–۵۷۲۷): `proj["html"]`, `proj['name']` — خطوط ۵۷۱۸، ۵۷۲۰
- `handle_web_add_element` (خط ۵۷۹۵–۵۸۴۲): `SELECT html` (۵۸۲۵)، `proj["html"]` (۵۸۳۰)، `UPDATE...SET html=` (۵۸۳۲)
- `handle_web_css_helper` (خط ۵۸۴۵–۵۸۹۴): `SELECT css` (۵۸۷۲)، `proj["css"]` (۵۸۷۴)، `UPDATE...SET css=` (۵۸۷۶)
- `handle_web_js_snippet` (خط ۵۸۹۷–۵۹۴۱): `SELECT js` (۵۹۱۹)، `proj["js"]` (۵۹۲۱)، `UPDATE...SET js=` (۵۹۲۳)
- هندلر `elif st == "web_add_text"` در `handle_message` (خط ۹۱۲۷–۹۱۴۸): `SELECT html` (۹۱۳۳)، `proj["html"]` (۹۱۳۷)، `UPDATE...SET html=` (۹۱۳۹)

از اونجایی که migration واقعاً ستون‌های `html`/`css`/`js`/`name` رو در جدول می‌سازه، این توابع دیگه با خطای «no such column» **کرش نمی‌کنن**. اما چون **بعد از لحظهٔ ساخت اولیهٔ پروژه هیچ‌جا دوباره این دو مجموعه ستون با هم sync نمی‌شن**، به‌محض اولین ویرایش واقعی پروژه (که از طریق ستون‌های `html_code`/`css_code`/`js_code` انجام می‌شه)، دو مجموعه ستون از هم واگرا می‌شن:

**سناریوی واقعی:** کاربر پروژه می‌سازه، محتوای واقعی HTML/CSS/JS خودش رو از طریق ویرایشگر اصلی (`web_edit_html` و مشابه) وارد می‌کنه — این محتوا در `html_code`/`css_code`/`js_code` ذخیره می‌شه و در «پیش‌نمایش» درست دیده می‌شه. اما وقتی کاربر روی «📦 دانلود ZIP»، «📄 دانلود HTML»، یا هر دکمهٔ «➕ المان»/«🎨 کمک CSS»/«⚡ قطعه JS» کلیک می‌کنه، این توابع از ستون‌های `html`/`css`/`js` می‌خونن که **همچنان مقدار خالی پیش‌فرض (`''`) یا محتوای قدیمیِ لحظهٔ ساخت رو دارن** — یعنی فایل دانلودی خالیه یا محتوای کاربر رو نداره، بدون هیچ پیام خطایی که دلیلش رو توضیح بده. این از یک کرش هم بدتره چون کاملاً خاموشه.

---

### ۱۰. اشتباه در state machine باعث می‌شه ساخت کد تخفیف «ساعتی» و «مناسبتی» هرگز کامل نشه

**محل:** `handle_message`، سه بلاک `elif` مرتبط با هم:
- بلاک عمومی (اشتباه، زودتر چک می‌شه): خط ۸۷۹۶–۸۸۱۱ — `elif st and st.startswith("create_discount_") and is_admin(user_id):`
- بلاک اختصاصی «ساعتی» (هرگز اجرا نمی‌شه): خط ۸۸۱۳–۸۸۲۲ — `elif st == "create_discount_hourly_hours" and is_admin(user_id):`
- بلاک اختصاصی «مناسبتی» (هرگز اجرا نمی‌شه): خط ۸۸۲۴–۸۸۳۳ — `elif st == "create_discount_occasion_date" and is_admin(user_id):`

پایتون شرط‌های `elif` رو به ترتیب از بالا به پایین چک می‌کنه و اولین match رو اجرا می‌کنه. چون `"create_discount_hourly_hours".startswith("create_discount_")` مقدار `True` برمی‌گردونه (و همین‌طور `"create_discount_occasion_date"`)، بلاک عمومی خط ۸۷۹۶ **همیشه زودتر از دو بلاک اختصاصی بعدی match می‌شه** — یعنی اون دو بلاک عملاً کد مرده و غیرقابل‌دسترسی (unreachable) هستن.

**جریان واقعی که می‌شکنه:**
۱. ادمین «ساخت کد تخفیف ساعتی» رو انتخاب می‌کنه → state به `create_discount_hourly` تنظیم می‌شه
۲. ادمین درصد تخفیف رو وارد می‌کنه → بلاک ۸۷۹۶ درست match می‌شه (`dtype="hourly"`, در مجموعهٔ مجاز هست) → state به `create_discount_hourly_hours` تنظیم می‌شه، از ادمین «تا چند ساعت دیگر منقضی شود؟» پرسیده می‌شه
۳. ادمین عدد ساعت (مثلاً «۲۴») رو وارد می‌کنه → **این پیام هم دوباره اول با بلاک ۸۷۹۶ چک می‌شه** (نه بلاک ۸۸۱۳ که برای همین حالت نوشته شده) → `dtype = "create_discount_hourly_hours".replace("create_discount_", "") = "hourly_hours"` → `"hourly_hours"` در مجموعهٔ `("special", "hourly", "occasion")` نیست → کد وارد شاخهٔ `else` می‌شه و پیام اشتباه «❌ عدد درصد معتبر وارد کنید.» نشون داده می‌شه (که کاملاً بی‌ربط به چیزیه که واقعاً از ادمین خواسته شده بود)

**نتیجه:** ادمین در یک حلقهٔ بی‌پایان با پیام خطای گیج‌کننده گیر می‌کنه و **هرگز نمی‌تونه یک کد تخفیف ساعتی یا مناسبتی بسازه.** فقط نوع «ویژه» (special) که تک‌مرحله‌ایه، کار می‌کنه.

---

### ۱۱. عدم HTML-escape در `colorize_line` (تابع تولید PDF/لاگ رنگی ترمینال) — رندر شکسته

**محل:** `colorize_line` (نزدیک خط ۵۲۱۹–۵۲۳۵)

این تابع خطوط کد پایتون رو برای نمایش رنگی HTML آماده می‌کنه، اما برخلاف تابع مشابه `generate_html_log` (خط ۱۰۰۱–۱۰۰۵ که با `.replace("<", "&lt;").replace(">", "&gt;")` این کاراکترها رو escape می‌کنه)، `colorize_line` **این escape رو انجام نمی‌ده**. تست مستقیم نشون داد که یک خط ساده مثل `if a < b and c > d:` باعث میشه کاراکترهای `<` و `>` خام مستقیماً وارد HTML بشن.

**تأثیر:** هر خط کد پایتون که شامل عملگرهای مقایسه (`<`, `>`, `<=`, `>=`) باشه — که در کد پایتون بسیار رایجه — باعث می‌شه خروجی HTML (لاگ رنگی ترمینال که برای کاربر ارسال می‌شه) به‌هم بریزه؛ مرورگر `<` رو شروع یک تگ ناقص تفسیر می‌کنه و ممکنه بخشی از محتوای بعدی صفحه رو مخفی نگه داره تا به یک `>` بعدی برسه.

---

### ۱۲. نصب خودکار کتابخانه از `requirements.txt` هیچ محدودیت پلنی نداره

**محل:** `auto_install_bot_requirements` (حوالی خط ۱۹۵۰–۲۰۵۰، دقیق‌تر باید با `grep` پیدا بشه اگه لازم شد)

نصب دستی یک کتابخانه (از طریق `handle_install_lib` → `check_plan_limit(user_id, "libraries")`) به‌درستی سقف `max_libraries` پلن کاربر رو رعایت می‌کنه. اما `auto_install_bot_requirements` که وقتی کاربر یک فایل `requirements.txt` همراه با کدش آپلود می‌کنه اجرا می‌شه، **کاملاً مستقل از این سیستم محدودیته** و هیچ چک `check_plan_limit` نداره. کاربر می‌تونه با گذاشتن یک `requirements.txt` طولانی، تعداد نامحدودی کتابخانه نصب کنه و کاملاً سقف پلن خودش رو دور بزنه.

---

## 🟡 باگ‌های متوسط

### ۱۳. `_get_domain` با استفاده اشتباه از `str.lstrip()` دامنه‌ها رو خراب می‌کنه

**محل:** `_get_domain` (خط ۶۱۰۳–۶۱۰۸)

```python
return urllib.parse.urlparse(url).netloc.lower().lstrip("www.")
```

`str.lstrip(chars)` آرگومانش رو به‌عنوان **یک مجموعه کاراکتر** برای حذف از ابتدای رشته در نظر می‌گیره، نه یک پیشوند دقیق. تست مستقیم این مثال‌ها رو تأیید کرد:

| ورودی | خروجی واقعی | خروجی درست |
|---|---|---|
| `weather.com` | `eather.com` ❌ | `weather.com` |
| `www3.example.com` | `3.example.com` ❌ | `www3.example.com` |
| `wwwexample.com` | `example.com` ❌ | `wwwexample.com` |

این تابع در `handle_web_scrape_url_input` (خط ۶۲۱۶ و ۶۲۳۷) برای تطبیق با دامنه‌های تأییدشده (`approved_sites`) و برای چک لیست سیاه آدرس‌های داخلی/خصوصی (خط ۶۲۲۳–۶۲۲۷) استفاده می‌شه. چون همون تابع باگ‌دار هم موقع ذخیره و هم موقع جستجو استفاده می‌شه، تطبیق داخلی سیستم خراب نمی‌شه، اما:
- دامنه‌هایی که ادمین تأیید می‌کنه، در دیتابیس و در پیام‌های تأیید/رد (خط ۶۳۰۶، ۶۳۵۱) به‌شکل خراب‌شده (`eather.com` به‌جای `weather.com`) نمایش داده می‌شن — گیج‌کننده برای ادمین.
- دامنه‌های قانونی که به‌طور اتفاقی با کاراکترهای `w`, `.` شروع می‌شن ممکنه به‌اشتباه در لیست سیاه بیفتن یا تطبیق نادرست پیدا کنن.

---

### ۱۴. هر دو تابع Broadcast (قدیمی و «بهینه‌شده») برای هر کاربر یک ترد OS جداگانه می‌سازن

**محل:** `send_broadcast` (خط ۵۵۷–۵۸۶) و `_broadcast_to_all_users` (خط ۵۹۳–۶۷۲)

هر دو تابع یک `threading.Semaphore` دارن (به‌ترتیب با سقف ۲۰ و ۸) که ارسال **همزمان** پیام رو محدود می‌کنه، اما هر دو، **قبل از اعمال این محدودیت**، یک شیء `threading.Thread` جداگانه برای **هر کاربر در دیتابیس** می‌سازن و همه رو بلافاصله `start()` می‌کنن:

```python
threads = [threading.Thread(target=worker, args=(uid,), daemon=True) for uid in users]
for t in threads:
    t.start()
```

Semaphore فقط جلوی اجرای همزمان بدنهٔ تردها رو می‌گیره، نه جلوی *ساخت* خودِ اشیای Thread رو. با یک پایگاه کاربری بزرگ (چند هزار نفر)، این باعث ساخت هم‌زمان هزاران ترد سطح سیستم‌عامل می‌شه که هرکدوم حافظهٔ پشته (stack) مصرف می‌کنن — می‌تونه به مصرف چند گیگابایتی حافظه یا خطای `RuntimeError: can't start new thread` منجر بشه، خیلی قبل از اینکه محدودیت نرخ ارسال واقعاً گلوگاه بشه.

جالبه که در جای دیگهٔ همین فایل (`main()`، خط ۹۲۴۳: `ThreadPoolExecutor(max_workers=16)`) از یک الگوی بوردار درست استفاده شده — هر دو تابع broadcast می‌تونستن به همین شکل با یک pool محدود بازنویسی بشن.

---

### ۱۵. `set_state` زودهنگام در بازکردن جزئیات تیکت — پیام‌های بعدی ادمین ممکنه به‌اشتباه به‌عنوان پاسخ تیکت ارسال بشن

**محل:** `handle_admin_view_ticket` (خط ۴۷۵۵–۴۷۹۰، دقیقاً خط ۴۷۸۱) و بلاک متناظرش در `handle_message`: `elif st == "admin_reply_ticket"` (خط ۸۷۰۶–۸۷۱۷)

به‌محض اینکه ادمین صرفاً برای **مشاهدهٔ جزئیات** یک تیکت کلیک می‌کنه (نه دکمهٔ «پاسخ دادن»)، خط ۴۷۸۱ بلافاصله `set_state(admin_id, "admin_reply_ticket", {...})` رو صدا می‌زنه. این یعنی از همون لحظه، **هر پیام متنی بعدی که ادمین بفرسته** (حتی اگه قصدش پاسخ به اون تیکت نبوده — مثلاً می‌خواد کار دیگه‌ای انجام بده) به‌طور خودکار به‌عنوان پاسخ رسمی تیکت پردازش و برای کاربر ارسال می‌شه (خط ۸۷۱۵: `send_message(target_user_id, ...)`).

راه‌حل درست اینه که `set_state` فقط زمانی صدا زده بشه که ادمین واقعاً دکمهٔ «💬 پاسخ دادن» رو بزنه (که در dispatcher، خط ۸۰۲۴–۸۰۲۹، جدا از این جا هم `set_state` رو صدا می‌زنه — یعنی این دو مسیر همزمان و به‌طور تکراری این کار رو انجام می‌دن، یکی زودتر از موقعش).

---

### ۱۶. برخورد بین حساب رایگان و حساب امتیازی در قرعه‌کشی (نکتهٔ طراحی قابل بحث)

**محل:** `handle_join_lottery` (خط ۴۸۵۷ به بعد) در مقابل `handle_spend_lottery` (خط ۴۹۷۴–۵۰۱۳)

کاربر می‌تونه هم از مسیر رایگان (یک شانس، یک‌بار) و هم از مسیر خرج امتیاز (چند شانس، با خرج امتیاز) در یک قرعه‌کشی شرکت کنه، و هیچ چکی جلوی استفادهٔ همزمان از هر دو مسیر رو نمی‌گیره. ممکنه این عمداً به‌عنوان «شانس اضافه» طراحی شده باشه، ولی چون هیچ کامنتی این رو تأیید نمی‌کنه، به‌عنوان یک نکتهٔ قابل بررسی ثبت می‌شه.

---

## ⚪ نکات جزئی و کد بو (Code Smell)

### ۱۷. کد مرده صریحاً علامت‌گذاری‌شده در انتهای فایل

**محل:** خط ۹۲۶۹–۹۳۸۲

خودِ نویسنده با کامنت `# ── توابع اضافی (کد مرده — برای بررسی) ──` (خط ۹۲۶۹) این بخش رو علامت‌گذاری کرده. پنج تابع در این بخش (`_handle_web_project_name` خط ۹۲۸۲، `_handle_web_group_name` خط ۹۳۰۶، `_handle_enterprise_company_name` خط ۹۳۲۳، `_handle_enterprise_description` خط ۹۳۳۲، `_handle_enterprise_user_count` خط ۹۳۴۷) با `grep` تأیید شد که **هیچ‌جای دیگری از فایل صدا زده نمی‌شن.**

این توابع نسخه‌های قدیمی‌تر و واگراشدهٔ منطقی هستن که الان به‌شکل inline در `elif`-chain تابع `handle_message` زندگی می‌کنه. نکتهٔ مهم: `_handle_web_project_name` (نسخهٔ مرده) فقط ستون‌های `html_code`/`css_code`/`js_code`/`project_name` رو پر می‌کنه، **نه** ستون‌های legacy (`html`/`css`/`js`/`name`) که در باگ #۹ بحث شد. اگه یک توسعه‌دهندهٔ آینده به‌اشتباه این تابع مرده رو دوباره وصل کنه (به‌جای نسخهٔ زندهٔ inline)، باگ #۹ دوباره و به شکل بدتری (کرش واقعی به‌جای خالی‌بودن خاموش) برمی‌گرده.

**پیشنهاد:** این پنج تابع حذف بشن تا از این ریسک و از حجم اضافهٔ فایل جلوگیری بشه.

---

### ۱۸. متغیرهای global حیاتی، خیلی دیرتر از جایی که استفاده می‌شن تعریف شدن

**محل:** تعریف در خط ۲۰۵۳–۲۰۵۸ (`bot_processes`, `bot_log_msgs`, `bot_restart_count`, `bot_last_restart`, `MAX_RESTART_ATTEMPTS`, `RESTART_COOLDOWN`)، اما اولین استفاده در `start_bot_process` و `watchdog_monitor` حوالی خط ۱۰۹۰–۱۲۵۰ اتفاق می‌افته — یعنی حدود ۹۰۰ خط **قبل از** تعریف‌شون.

چون این‌ها متغیرهای سطح-ماژول هستن و پایتون کل فایل رو قبل از فراخوانی `main()` parse و اجرا می‌کنه، این کد **در عمل کرش نمی‌کنه**. اما این ترتیب به‌شدت شکننده و گمراه‌کننده‌ست: اگه این توابع در آینده به یک ماژول جدا منتقل بشن، یا با import جزئی فراخوانی بشن، بلافاصله `NameError` می‌گیرن. پیشنهاد می‌شه این تعریف‌ها به بالای فایل منتقل بشن.

---

### ۱۹. عدم رعایت محدودیت پلن به‌صورت لحظه‌ای در انتخاب نام کتابخانه

**محل:** `handle_install_lib` (چک اولیه، خط ۲۱۷۷–۲۲۰۰) در مقابل `handle_install_lib_name` (مرحلهٔ نهایی، خط ۲۲۰۳ به بعد)

چک محدودیت پلن (`check_plan_limit`) فقط در مرحلهٔ ورود به state انجام می‌شه، نه دوباره در لحظهٔ نهایی که کاربر واقعاً نام کتابخانه رو وارد می‌کنه. اگه بین این دو لحظه (که ممکنه طولانی باشه، چون منتظر تایپ کاربره) به سقف پلن برسه (مثلاً از تب دیگه‌ای)، این چک دوباره انجام نمی‌شه. یک race condition خفیف با احتمال وقوع کم.

---

### ۲۰. `weekly_scheduler` ریسک از‌دست‌دادن پنجرهٔ اجرای هفتگی

**محل:** `weekly_scheduler` (نزدیک خط ۲۸۶۰–۲۸۷۰، با `time.sleep(30)` بین هر چک)

شرط اجرا `now.weekday() == 5 and now.hour == 9 and now.minute == 0` هست، اما چک با فاصلهٔ هر ۳۰ ثانیه انجام می‌شه در حالی که پنجرهٔ هدف («دقیقهٔ صفر») فقط ۶۰ ثانیه طوله. بسته به فاز دقیق (offset) چک‌ها نسبت به مرز دقیقه، این پنجره ممکنه به‌ندرت به‌طور کامل رد بشه و اجرای هفتگی یک هفته به تعویق بیفته. ریسک واقعی ولی نه قطعی.

---

### ۲۱. عدم `os.kill` fallback هنگام حذف ربات باعث می‌مونه پروسه‌های سیستمی زنده بمونن

**محل:** `handle_admin_delete_bot` (نزدیک خط ۳۳۰۵) در مقابل `handle_delete_bot_final` (خط ۱۵۴۷، که از طرف خود کاربر صدا زده می‌شه)

نسخهٔ ادمین فقط `proc.terminate()` (که سیگنال SIGTERM قابل‌نادیده‌گرفتن می‌فرسته) رو صدا می‌زنه، در حالی که نسخهٔ کاربر از `os.kill(bot["pid"], 9)` (SIGKILL، غیرقابل‌نادیده‌گرفتن) استفاده می‌کنه. این ناهماهنگی، به‌علاوهٔ مشکل کلی‌تر «`bot_processes` فقط در حافظه» (باگ #۸)، باعث می‌شه حذف ربات از پنل ادمین قابل‌اطمینان‌تر از حذف توسط خود کاربر نباشه.

---

## خلاصهٔ نهایی

| # | عنوان | شدت | تعداد تابع درگیر |
|---|---|---|---|
| ۱ | اجرای بدون sandbox کد کاربر | 🔴 | ۱ |
| ۲ | قیمت پلن بعد از ری‌استارت به مقدار اشتباه برمی‌گرده | 🔴 | ۲ |
| ۳ | پرداخت Enterprise کاملاً شکسته | 🔴 | ۱ |
| ۴ | تخفیف ۱۰۰٪ → فاکتور با مبلغ صفر | 🔴 | ۲ |
| ۵ | حذف پروژهٔ وب می‌تونه پروژهٔ اشتباه رو حذف کنه | 🔴 | ۲ |
| ۶ | «کتابخانه اختصاصی» کاملاً غیرفعال | 🟠 | ۱ (dead) |
| ۷ | دکمه‌های قالب پروژهٔ وب کار نمی‌کنن | 🟠 | ۲ |
| ۸ | توقف/ری‌استارت ربات بعد از ری‌استارت سرور کار نمی‌کنه | 🟠 | ۵ |
| ۹ | ستون‌های موازی `web_projects` → از دست رفتن خاموش محتوا | 🟠 | ۷ |
| ۱۰ | ساخت کد تخفیف ساعتی/مناسبتی هرگز کامل نمی‌شه | 🟠 | ۳ (تداخل) |
| ۱۱ | عدم escape در `colorize_line` → رندر شکسته | 🟠 | ۱ |
| ۱۲ | نصب خودکار کتابخانه بدون محدودیت پلن | 🟠 | ۱ |
| ۱۳ | باگ `lstrip("www.")` در تشخیص دامنه | 🟡 | ۱ |
| ۱۴ | ساخت ترد نامحدود در هر دو تابع Broadcast | 🟡 | ۲ |
| ۱۵ | `set_state` زودهنگام برای پاسخ تیکت | 🟡 | ۲ |
| ۱۶ | برخورد مسیر رایگان/امتیازی قرعه‌کشی | 🟡 | ۲ |
| ۱۷ | کد مرده صریح در انتهای فایل | ⚪ | ۵ |
| ۱۸ | متغیرهای global دیر-تعریف‌شده | ⚪ | ۶ متغیر |
| ۱۹ | عدم چک لحظه‌ای محدودیت پلن کتابخانه | ⚪ | ۲ |
| ۲۰ | ریسک از‌دست‌دادن پنجرهٔ `weekly_scheduler` | ⚪ | ۱ |
| ۲۱ | عدم `os.kill` fallback در حذف ادمین | ⚪ | ۱ |

**جمع کل: ۲۱ یافتهٔ مجزا** (۵ حیاتی، ۷ مهم، ۴ متوسط، ۵ جزئی)

---

*نکته: طبق درخواست، هیچ باگی از بخش «سیستم API Keys کدبات» در این گزارش نیامده، چون قراره بازنویسی بشه.*
